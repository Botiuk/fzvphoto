<div class="container text-start mb-3 mt-1">
  <div class="row justify-content-end">
    <div class="col"><h4>
        <%= t('.title_part1') %> "<%= Post.human_enum_name(:posttype, @posttype) %>"
            <% if @posts.empty? %>
              <%= t('.title_empty') %>
            <% else %>
              (<%= t('.title_part2') %> <%= @count %>)
            <% end %>
        </h4></div>
  </div>
</div>

<%= render 'shared/searchposts' %>
